{
  "name": "micro-vue-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "lerna bootstrap",
    "clean": "lerna clean",
    "dev:main": "bash ./main.sh",
    "dev:login": "bash ./login.sh",
    "dev:admin": "bash ./admin.sh",
    "build": "lerna run build && npm run commit",
    "commit": "git add . && git commit -m \"merge\" -n && git push -u origin master",
    "lint": "vue-cli-service lint"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
